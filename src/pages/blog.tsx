import React from "react";
import Template from "@/components/Template";

const Blog: React.FC = () => {
  return (
    <Template>
      <h1>Substack here.</h1>
    </Template>
  );
};

export default Blog;